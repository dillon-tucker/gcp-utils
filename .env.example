# GCP Utilities Configuration Example
# Copy this file to .env and fill in your values

# Required: Your GCP project ID
GCP_PROJECT_ID=my-gcp-project

# Optional: Path to service account JSON credentials
# If not set, uses Application Default Credentials
GCP_CREDENTIALS_PATH=/path/to/service-account.json

# Optional: Default GCP location/region
GCP_LOCATION=us-central1

# Optional: Default Cloud Storage bucket
GCP_STORAGE_BUCKET=my-default-bucket

# Optional: Firestore database ID
GCP_FIRESTORE_DATABASE=(default)

# Optional: Cloud Run region
GCP_CLOUD_RUN_REGION=us-central1

# Optional: Workflows location
GCP_WORKFLOWS_LOCATION=us-central1

# Optional: Cloud Tasks location
GCP_CLOUD_TASKS_LOCATION=us-central1

# Optional: Prefix for Pub/Sub topics
GCP_PUBSUB_TOPIC_PREFIX=

# Optional: Enable detailed request logging (true/false)
GCP_ENABLE_REQUEST_LOGGING=false

# Optional: Default timeout for operations (seconds, 1-3600)
GCP_OPERATION_TIMEOUT=300
